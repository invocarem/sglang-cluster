# Frontend (Vite + React)

## Quick start

1. Install dependencies:
   - `npm install`
2. Optional API base override:
   - `echo 'VITE_API_BASE=http://localhost:18080' > .env`
3. Run:
   - `npm run dev`
